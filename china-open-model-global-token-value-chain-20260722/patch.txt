<script>
(function(){
  var nav=document.querySelector('nav');
  if(nav&&!nav.querySelector('a[href="#modelcompare"]')){
    var link=document.createElement('a');
    link.href='#modelcompare';
    link.textContent='五模型对比';
    var before=nav.querySelector('a[href="#kimi"]');
    nav.insertBefore(link,before||null);
  }
  var cells=document.querySelectorAll('.hero .stats .stat');
  var items=[
    ['18.9%','智谱开放平台及API<br>2025年毛利率'],
    ['约50%','Fireworks<br>非审计毛利率估计'],
    ['57.5倍','五模型最高与最低<br>输出Token标价差'],
    ['26家','GLM 5.2<br>OpenRouter Provider数量']
  ];
  for(var i=0;i<cells.length&&i<items.length;i++){
    var num=cells[i].querySelector('.num');
    var lab=cells[i].querySelector('.lab');
    if(num)num.textContent=items[i][0];
    if(lab)lab.innerHTML=items[i][1];
  }
  document.addEventListener('DOMContentLoaded',function(){
    var psNote=document.querySelector('.ps-chart .legend-note');
    if(psNote&&!/汇率假设/.test(psNote.textContent)){
      psNote.innerHTML+=' 粗略换算采用USD/HKD≈7.8、USD/CNY≈7.2；市场价值与汇率波动会使倍数变化。';
    }
    document.querySelectorAll('.sources a').forEach(function(a){
      if(/quote_ci_basicdata\.php\?code=2513/.test(a.href)) a.href='https://content.etnet.com.hk/content/cpyrevamp/eng/stock_quote.php?code=02513';
      if(/quote_ci_basicdata\.php\?code=100/.test(a.href)) a.href='https://content.etnet.com.hk/content/cpyrevamp/eng/stock_quote.php?code=00100';
    });
  });
})();
</script>
