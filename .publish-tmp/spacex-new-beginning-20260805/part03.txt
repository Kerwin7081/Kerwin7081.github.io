ar(--sans);color:var(--green)}
    .monitor-item span{font:13px/1.55 var(--sans);color:var(--muted)}
    .callout{margin-top:20px;border-left:5px solid var(--gold);background:#fff7ed;padding:18px 20px}
    .callout h3{font-size:19px;margin:0 0 7px;color:var(--green)}
    .callout p{font-size:14px;line-height:1.7;margin:0;color:#645b50}
    .sources{display:grid;grid-template-columns:1.1fr .9fr;gap:26px}
    .source-list{margin:0;padding-left:20px}
    .source-list li{margin:8px 0;font:13px/1.65 var(--sans);color:var(--muted)}
    .source-list a{color:var(--green);text-underline-offset:3px}
    .related{display:grid;gap:10px}
    .related a{display:block;text-decoration:none;border:1px solid var(--line);background:white;padding:15px}
    .related b{display:block;font-size:15px;color:var(--green);margin-bottom:4px}
    .related span{font:12px/1.5 var(--sans);color:var(--muted)}
    .enya{padding:34px 44px;background:var(--green);color:white;display:grid;grid-template-columns:1fr 1fr;gap:26px}
    .enya h2{font-size:26px;margin:0 0 8px}
    .enya p{margin:0;color:#dce8e3;font-size:14px;line-height:1.75}
    .roles{display:grid;gap:10px}
    .role{border-top:1px solid rgba(255,255,255,.28);padding-top:10px;font:13px/1.6 var(--sans)}
    .role b{color:#f0d49e;margin-right:8px}
    footer{padding:18px 44px;display:flex;justify-content:space-between;gap:20px;font:11px/1.55 var(--sans);color:var(--muted)}
    @media(max-width:900px){
      .cover{grid-template-columns:1fr}.cover-side{min-height:250px}
      .fact-grid{grid-template-columns:1fr 1fr}.flywheel{grid-template-columns:1fr 1fr}.wheel-node:not(:last-child)::after{display:none}
      .layer{grid-template-columns:46px 165px 1fr}.layer .status{grid-column:2/4;text-align:left}
      .map-grid,.scenario-grid{grid-template-columns:1fr}.sources,.enya{grid-template-columns:1fr}
    }
    @media(max-width:640px){
      body{font-size:16px;line-height:1.8}
      .access-gate{padding:10px;align-items:start;overflow:auto}
      .access-gate__paper{margin:14px 0}
      .access-gate__mast,.access-gate__foot{padding:9px 12px;font-size:9px}
      .access-gate__body{grid-template-columns:72px minmax(0,1fr);gap:14px;padding:24px 14px}
      .seal{width:68px;height:68px;font-size:15px}.seal small{font-size:7px}
      .access-gate h1{font-size:25px}.access-gate p{font-size:13px;line-height:1.6}
      .gate-field{grid-template-columns:minmax(0,1fr) 78px}
      .site-shell{width:100%;margin:0;border-left:0;border-right:0;box-shadow:none}
      .topbar{padding:8px 12px;font-size:9px}.masthead{padding:16px 14px;align-items:start}.brand{font-size:21px}.mast-meta{font-size:9px}
      .cover-copy{padding:36px 18px 30px}.cover h1{font-size:clamp(24px,8.2vw,28px);line-height:1.12}.cover-deck{font-size:17px;line-height:1.62}
      .cover-side{padding:25px 18px;min-height:220px}.cover-side h2{font-size:22px}
      .scope{margin:16px 14px 0;padding:13px 14px}
      .section{padding:36px 16px}.section-head{grid-template-columns:1fr;gap:4px;margin-bottom:20px}.section-num{padding-top:0}
      .section h2{font-size:clamp(23px,7vw,26px)}
      .verdict{grid-template-columns:1fr}.verdict-main,.verdict-side{padding:21px}
      .fact-grid{grid-template-columns:1fr 1fr}.fact{padding:15px 11px;min-height:118px}.fact strong{font-s