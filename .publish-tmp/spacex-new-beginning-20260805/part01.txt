  .topbar a{text-decoration:none;color:var(--green)}
    .masthead{display:flex;justify-content:space-between;gap:20px;align-items:end;padding:22px;border-bottom:1px solid var(--line)}
    .brand{font:700 25px/1 var(--serif);color:var(--green)}
    .brand small{display:block;margin-top:6px;font:700 9px/1.3 var(--sans);letter-spacing:.15em;text-transform:uppercase;color:var(--muted)}
    .mast-meta{text-align:right;font:700 11px/1.6 var(--sans);color:var(--muted)}
    .cover{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(280px,.75fr);border-bottom:1px solid var(--line)}
    .cover-copy{padding:58px 44px 48px}
    .cover h1{font-size:clamp(34px,5vw,49px);line-height:1.08;letter-spacing:-.025em;margin:14px 0 18px;color:var(--green)}
    .cover-deck{max-width:760px;font-size:20px;line-height:1.66;margin:0;color:#3d463f}
    .cover-tags{display:flex;flex-wrap:wrap;gap:8px;margin-top:26px}
    .cover-tags span{border:1px solid #b9c8bf;background:var(--soft);padding:5px 9px;font:700 11px/1.3 var(--sans);color:var(--green)}
    .cover-side{background:var(--green);color:white;padding:34px 28px;display:flex;flex-direction:column;justify-content:space-between}
    .cover-side h2{font-size:25px;line-height:1.28;margin:8px 0 12px}
    .cover-side p{font-size:14px;line-height:1.75;color:#dfe9e4;margin:0}
    .signal{border-top:1px solid rgba(255,255,255,.28);padding-top:18px;margin-top:26px}
    .signal b{display:block;font:700 11px/1.4 var(--sans);letter-spacing:.12em;text-transform:uppercase;color:#e7c98e;margin-bottom:7px}
    .scope{margin:24px 44px 0;padding:16px 18px;border:1px solid var(--line);background:#fff7ef;font:14px/1.75 var(--sans);color:#635b53}
    .section{padding:48px 44px;border-bottom:1px solid var(--line)}
    .section-head{display:grid;grid-template-columns:90px minmax(0,1fr);gap:24px;margin-bottom:26px}
    .section-num{font:700 12px/1.4 var(--sans);letter-spacing:.13em;color:var(--gold);padding-top:9px}
    .section h2{font-size:clamp(28px,3.8vw,36px);line-height:1.2;letter-spacing:-.015em;margin:0 0 10px;color:var(--green)}
    .section-lead{margin:0;max-width:800px;color:var(--muted);font-size:16px;line-height:1.75}
    .verdict{display:grid;grid-template-columns:1.2fr .8fr;gap:18px}
    .verdict-main{background:var(--green);color:white;padding:28px}
    .verdict-main h3{font-size:26px;line-height:1.32;margin:0 0 14px}
    .verdict-main p{margin:0;color:#e0e9e4}
    .verdict-side{border:1px solid var(--line);padding:24px;background:white}
    .verdict-side b{display:block;color:var(--gold);font:700 11px/1.4 var(--sans);letter-spacing:.12em;text-transform:uppercase;margin-bottom:10px}
    .verdict-side p{margin:0;font-size:15px;line-height:1.75}
    .fact-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:22px 0 0}
    .fact{border:1px solid var(--line);background:white;padding:18px 14px;min-height:126px}
    .fact strong{display:block;font-size:27px;line-height:1.1;color:var(--green);margin-bottom:9px}
    .fact span{display:block;font:700 11px/1.45 var(--sans);color:var(--muted)}
    .fact small{display:block;margin-top:9px;font:11px/1.45 var(--sans);color:var(--gold)}
    .table-wrap{overflow-x:auto;border:1px solid var(--line);background:white}
    table{border-collapse:collapse;width:100%;min-